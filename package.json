{
  "name": "kotoba-lens",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome's local Prompt API rewrites the web in any voice you describe.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.2",
    "@types/dom-chromium-ai": "^0.0.15",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
